## To get started for the first time

```
// Root directory
docker-compose up -d

cd api
npm install
npm run start

// New terminal
cd front
npm install
npm run start
```

Go to http://localhost:8000
